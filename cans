<div class="dount-chart">
  <h6 style="text-align:center">change Request-Plant wise view</h6>
  <canvas baseChart
          [datasets]="doughnutChartData"
          [chartType]="doughnutChartTypes"
          [labels]="doughnutChartLabels"
          (chartHover)="chartHovered($event)"
          (chartClick)="chartClicked($event)"
          style="height: 290px; width: 390px; box-sizing: border-box;">
  </canvas>
</div>
Can't bind to 'chartTypes' since it isn't a known property of 'canvas'.
Can't bind to 'data' since it isn't a known property of 'canvas'.
