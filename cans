 checklist: any[] = [];
  selectedCategory: number = 0; // Assuming you're populating these values somewhere else
  plantId: number = 0; // Assuming you're populating these values somewhere else
  classificationId: number = 0; // Assuming you're populating these values somewhere else

  this.checklist = response.filter((item: { supportId: number; plantId: number; categoryId: number; classificationId: number; }) =>
          item.supportId === 1 &&
          item.categoryId === this.selectedCategory &&
          item.plantId === this.plantId &&
          item.classificationId === this.classificationId
        );
        console.log('CheckList:', this.checklist);
      },
