 navigateToPage(label: string) {
    switch (label) {
      case 'Label 1':
        this.router.navigate(['/page1']);
        break;
      case 'Label 2':
        this.router.navigate(['/page2']);
        break;
      case 'Label 3':
        this.router.navigate(['/page3']);
        break;
      default:
        console.log('No action defined for this label');
    }
  }
