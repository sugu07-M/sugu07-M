<div class="status-chart">
<ngx-charts-bar-horizontal-stacked
  [view]="view"
  [results]="multi"
  [gradient]="gradient"
  [xAxis]="showXAxis"
  [yAxis]="showYAxis"
  [showXAxisLabel]="showXAxisLabel"
  [showYAxisLabel]="showYAxisLabel"
  [xAxisLabel]="xAxisLabel"
  [yAxisLabel]="yAxisLabel"
  [showDataLabel]="showDataLabel"
  [showGridLines]="false"
  (select)="onSelect($event)" style="margin-left:2px" >
  
 
</ngx-charts-bar-horizontal-stacked></div>
.status-chart-container {
  width: 100%;
  overflow-x: auto; /* Horizontal scrollbar if needed */
}

.chart-style {
  margin-left: 2px;
}

/* Media Query for responsiveness */
@media only screen and (max-width: 768px) {
  .chart-style {
    /* Adjust styles for smaller screens */
    margin-left: 0;
  }
}


<div class="dount-chart">
  <h6 style="text-align:center">change Request-Plant wise view</h6>
  <canvas baseChart
          [datasets]="doughnutChartData"
          [labels]="doughnutChartLabels"
          (chartHover)="chartHovered($event)"
          (chartClick)="chartClicked($event)"
          style="height: 290px; width: 390px; box-sizing: border-box;">
  </canvas>
</div>
.dount-chart {
  position: relative;
  width: 100%; /* Ensure the chart fills its container */
  max-width: 500px; /* Define maximum width if needed */
  margin: 0 auto; /* Center the chart horizontally */
}
