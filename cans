  <div class="dropdown">
    <button class="btn" type="button" id="dropdownMenuButton" (click)="toggleLogout($event)" data-bs-toggle="dropdown" aria-expanded="false">
      <img src="assets/image/profile.png" alt="Profile" class="profile-img">
    </button>
    <ul *ngIf="showLogout">

      <li><a href="http://192.168.1.134:82/#/login"><i class='fas fa-sign-out-alt' style='font-size:15px; color:white;'></i>Logout</a></li>

    </ul>

  </div>
/* CSS to change the background color of the dropdown menu */
.dropdown ul {
    background-color: #f0f0f0; /* Change this to the desired background color */
}

/* CSS to change the background color of the logout button */
.dropdown .btn {
    background-color: transparent; /* Change this to the desired background color */
    border: none; /* Optional: Remove the border */
}

/* CSS to change the color of the logout button's icon */
.dropdown .btn img {
    filter: invert(0.5); /* Change this to the desired color */
}

/* CSS to change the background color of the dropdown items */
.dropdown ul li {
    background-color: #f0f0f0; /* Change this to the desired background color */
}

/* CSS to change the color of the text in the dropdown items */
.dropdown ul li a {
    color: black; /* Change this to the desired text color */
}
