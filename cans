
    onItemSelect(item: any) {
        this.selectedItems.push(item);
    }

    onSelectAll(items: any) {
        this.selectedItems = items;
    }

    onItemDeSelect(item: any) {
        this.selectedItems = this.selectedItems.filter(selectedItem => selectedItem !== item);
    }

    onUnSelectAll() {
        this.selectedItems = [];
    }
  const newApproverStage = this.selectedItems.map(item => item.item_id).join(',');
  if (this.approverstage !== newApproverStage) {
    this.approverstage = newApproverStage;
    console.log('Approver Stage:', this.approverstage);
  }Severity	Code	Description	Project	File	Line	Suppression State
Error	TS2339	(TS) Property 'item_id' does not exist on type 'string'.	D:\IT_Portal\IT-Portal\IT-Portal.UI\tsconfig.json	D:\IT_Portal\IT-Portal\IT-Portal.UI\src\app\master-support\support-team\support-team.component.ts	314	Active
