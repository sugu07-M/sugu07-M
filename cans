   <tr class="tr-body">
     <td style="color:Black;font-weight:500;">Open</td>
     <ng-container *ngFor="let plantId of code">
       <td (click)="navigateToOtherComponent()">{{ getopenCount('Open', plantId) }}</td>
     </ng-container>
     <td>{{ getTotalopen('Open') }}</td>
   </tr>
   <tr class="tr-body">
     <td style="color:Black;font-weight:500;">Completed</td>
     <ng-container *ngFor="let plantId of code">
       <td (click)="navigatecompleted()">{{ getStatusCount('Completed', plantId) }}</td>
     </ng-container>
     <td>{{ getTotalCount('Completed') }}</td>
   </tr>
