  <button class="logout-button" *ngIf="showLogout" [routerLink]="'/logout'">Logout</button>

showLogout: boolean = false;

  toggleLogout() {
    this.showLogout = !this.showLogout;
  }
(click)="toggleLogout()"
