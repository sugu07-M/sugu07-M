npm install @angular/cdk @swimlane/ngx-charts
import { DashboardComponent } from './dashboard/dashboard.component';
export class DashboardComponent {
  doughnutChartData: any[] = [...]; // Define your chart data
  doughnutChartType: string = 'doughnut'; // Define chart type
  doughnutChartLabels: string[] = [...]; // Define chart labels
}
