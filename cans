 onClick(event: any): void {
   if (this.barChartLabels = 'open') {
     this.router.navigate(['/filterdashboard'])
   } else if (this.barChartLabels ='completed'){
     this.router.navigate(['/filterdashboard'])
   }
  
 }
Type 'string' is not assignable to type 'string[]'.
