 public doughnutChartOptions: any = {
    responsive: true,
    legend: {
      position: 'right', // Adjust position as needed
      labels: {
        fontColor: 'black' // Adjust label color as needed
      }
    }
  };
