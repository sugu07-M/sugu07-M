
public chartClicked(event: any): void {
    if (event.active && event.active.length > 0) {
      const clickedLabel = this.doughnutChartData.labels[event.active[0]._index];
      this.navigateToPage(clickedLabel);
    }
  }

  private navigateToPage(label: string): void {
    switch (label) {
      case 'Category 1':
        this.router.navigate(['/categories', '1']); // Example: Navigate to category 1
        break;
      case 'Category 2':
        this.router.navigate(['/categories', '2']); // Example: Navigate to category 2
        break;
      // Add more cases for other labels as needed
      default:
        console.warn('Unhandled label:', label);
        break;
    }
  }
