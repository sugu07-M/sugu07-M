 formFields: FormField[] = [{ approve: '', selectedOption: 'Change Request' }];
  
  toggleMiniForm() {
    this.formFields.push({ approve: '', selectedOption: 'Change Request' });
  }

*ngFor="let field of formFields"  [(ngModel)]="field.selectedOption"
