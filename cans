<div style="width: 600px; text-align: center; margin-top: 20px;">
  <div>Doughnut Chart</div>
  <canvas ngChartjs [chartData]="doughnutChartData" [chartLabels]="doughnutChartLabels" [chartType]="doughnutChartType"
          (chartHover)="chartHovered($event)" (chartClick)="chartClicked($event)"></canvas>
</div><div style="width: 600px; text-align: center; margin-top: 20px;">
  <div>Doughnut Chart</div>
  <canvas ngChartjs [chartData]="doughnutChartData" [chartLabels]="doughnutChartLabels" [chartType]="doughnutChartType"
          (chartHover)="chartHovered($event)" (chartClick)="chartClicked($event)"></canvas>
</div>

 doughnutChartData: number[] = [350, 450, 100];
  doughnutChartLabels: string[] = ['Red', 'Green', 'Blue'];
  doughnutChartType: string = 'doughnut';

  chartHovered(event: any) {
    // Handle chart hover event if needed
  }

  chartClicked(event: any) {
    // Handle chart click event if needed
  }
 Can't bind to 'datasets' since it isn't a known property of 'canvas'.
NG8002: Can't bind to 'data' since it isn't a known property of 'canvas'.
NG8002: Can't bind to 'chartData' since it isn't a known property of 'canvas'.
