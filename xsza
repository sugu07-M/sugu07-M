 <div class="col">
   <label>Support Name</label>
   <!--<input type="text" placeholder="supportID" [(ngModel)]="supportsId" />-->
   <select [(ngModel)]="supportsId">
     <option value="1">Change Request</option>
     <option value="2">Help Disk</option>
     <option value="3">Project Management</option>
   </select>
 </div>
 <div class="col">
   <label>Category Name</label>
   <!--<input type="text" placeholder="categoryId" [(ngModel)]="categoryId" />-->
   <select [(ngModel)]="categoryId">
     <option value="" disabled selected>Select categoryName</option>
     <option *ngFor="let cldata of systemlandscape" [value]="cldata.categoryId">{{cldata.categoryName}}</option>
   </select>
 </div>

 <div class="col">
   <label>Classification Name</label>
   <!--<input type="text" placeholder="classificationId" [(ngModel)]="classificationId" />-->
   <select [(ngModel)]="classificationId">
     <option value="" disabled selected>Select classificationName</option>
     <option *ngFor="let cldata of systemlandscape" [value]="cldata.classificationId">{{cldata.classificationName}}</option>
   </select>
 </div>
 <div class="row mt-2">
   <div class="col">
     <label>Priority Name</label>
     <!--<input type="text" placeholder="priorityNumber" [(ngModel)]="priorityNumber" />-->
     <select [(ngModel)]="priorityNumber">
       <option value="" disabled selected>Select PriorityName</option>
       <option *ngFor="let cldata of systemlandscape" [value]="cldata.priorityNumber">{{cldata.priorityName}}</option>
     </select>
   </div>
today: any;
SystemLandscapeId: any[] = [];
systemLandscape: any[] = [];
classificationId: any[] = [];
priorityNumber: any[] = [];
isactive: Boolean = true;
supportsId: any[] = [];
categoryId: any[] = [];

PostSystemLandscapeSp() {

  const apiUrl = this.apiurl + '/SystemLandscapeSp/PostSystemLandscape'
  const requestBody = {
    "flag": "I",
    "sysLandscapeId": 0,
    "supportId": this.supportsId,
    "categoryId": this.categoryId,
    "classificationId": this.classificationId,
    "sysLandscape1": this.systemLandscape,
    "priorityNum": this.priorityNumber,
    "isActive": this.isactive,
    "createdBy": this.supportid,
    "createdDt": this.today,
    "modifiedBy": this.supportid,
    "modifiedDt": this.today

  }
  const httpOptions = {
    headers: new HttpHeaders({
      'Content-Type': 'application/json'
    })
  };
  this.http.post(apiUrl, requestBody, httpOptions).subscribe(
    (response: any) => {
      console.log(response);
      alert("Saved Successfully!");
      this.router.navigate(['/systemlandscape']);
    },
    (error: any) => {
      console.log('Post request failed', error);
    });
}
