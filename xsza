<canvas *ngIf="doughnutChartData"
        baseChart
        [data]="doughnutChartData[0].datasets[0].data"
        [labels]="doughnutChartData[0].datasets[0].label"
        [chartType]="'doughnut'"
        [backgroundColor]="doughnutChartData[0].datasets[0].backgroundColor"
        (chartHover)="chartHovered($event)"
        (chartClick)="chartClicked($event)"></canvas>
