public barChartData: any[] = [{ data: [], label: 'changerequest' }];

// Call updateChartData whenever your changerequest data changes
// For example, after receiving the HTTP response
this.http.get(apiUrls, requestBody).subscribe(
  (response: any) => {
    this.changerequest = response;
    this.updateChartData();
  }
);

public updateChartData(): void {
  // Filter changerequest based on month and year
  const filteredData = this.changerequest.filter((item: any) => {
    const crDate = new Date(item.crdate);
    return crDate.getFullYear() === selectedYear && crDate.getMonth() === selectedMonth;
  });

  // Count the total number of changerequest for each month
  const counts = {};
  filteredData.forEach((item: any) => {
    const crDate = new Date(item.crdate);
    const key = `${crDate.getMonth() + 1}-${crDate.getFullYear()}`; // Month-Year format
    counts[key] = (counts[key] || 0) + 1;
  });

  // Generate data for bar chart
  const data = this.barChartLabels.map((label: string) => {
    const key = label.split('-').reverse().join('-'); // Reversing the label to match with counts
    return counts[key] || 0; // If no data found for this month-year, default to 0
  });

  // Update barChartData
  this.barChartData[0].data = data;
}

X [ERROR] TS2304: Cannot find name 'selectedYear'. [plugin angular-compiler]

    src/app/dashboard/dashboard.component.ts:234:38:
      234 │ ...crDate.getFullYear() === selectedYear && crDate.getMonth() ===...
          ╵                             ~~~~~~~~~~~~


X [ERROR] TS2304: Cannot find name 'selectedMonth'. [plugin angular-compiler]

    src/app/dashboard/dashboard.component.ts:234:76:
      234 │ ...llYear() === selectedYear && crDate.getMonth() === selectedMonth;
          ╵                                                       ~~~~~~~~~~~~~


X [ERROR] TS7053: Element implicitly has an 'any' type because expression of type 'string' can't be used to index type '{}'.
  No index signature with a parameter of type 'string' was found on type '{}'. [plugin angular-compiler]

    src/app/dashboard/dashboard.component.ts:242:6:
      242 │       counts[key] = (counts[key] || 0) + 1;
          ╵       ~~~~~~~~~~~


X [ERROR] TS7053: Element implicitly has an 'any' type because expression of type 'string' can't be used to index type '{}'.
  No index signature with a parameter of type 'string' was found on type '{}'. [plugin angular-compiler]

    src/app/dashboard/dashboard.component.ts:242:21:
      242 │       counts[key] = (counts[key] || 0) + 1;
          ╵                      ~~~~~~~~~~~


X [ERROR] TS7053: Element implicitly has an 'any' type because expression of type 'string' can't be used to index type '{}'.
  No index signature with a parameter of type 'string' was found on type '{}'. [plugin angular-compiler]

    src/app/dashboard/dashboard.component.ts:250:13:
      250 │       return counts[key] || 0; // If no data found for this month...
          ╵              ~~~~~~~~~~~
