interface ChartData<T, U, V> {
    data: U[];
    label: T;
    backgroundColor: V[];
}
 Type '{ data: number[]; label: string; backgroundColor: string[]; }[]' is not assignable to type 'ChartData<"doughnut", number[], string>'.
