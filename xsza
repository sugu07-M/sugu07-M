
  chartdata: any;
  completed: any[]=[];
  noncompleted: any[]=[];
  month: any[] = [];
  barChartData: any[] = [];
  barChartLabels: string[] = [];
  barChartPlugins = [pluginDataLabels];
  getBarchart() {
    const apiUrls = this.apiurl + '/Barchart/Getbarchart';

    const httpOptions = {
      headers: new HttpHeaders({
        'content-Type': 'application/json'
      })
    };

    this.http.get(apiUrls).subscribe(
      (response: any) => {
        this.chartdata = response;

        // Extracting data
        this.completed = response.map((item: { completedIdnum: any; }) => item.completedIdnum);
        this.noncompleted = response.map((item: { nonCompletedIdnum: any; }) => item.nonCompletedIdnum);
        const month = response.map((item: { crmonth: any }) => item.crmonth);

        // Update chart data and labels
        this.updateBarChartLabels(month);
        this.updateBarChartData();

      },
      (error) => {
        console.log("Post failed", error);
      }
    );
  }

  updateBarChartData() {
    this.barChartData = [
      { data: this.noncompleted, label: 'Open' },
      { data: this.completed, label: 'Completed' }
    ];
  }

  updateBarChartLabels(month: number[]) {
    this.barChartLabels = month.map(month => this.getMonthName(month));
  }

  getMonthName(month: number): string {
    const monthNames = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];
    return monthNames[month - 1];
  }
  public barChartPlugins: Plugin[] = [pluginDataLabels]

  public barChartType: ChartType = 'bar';

  public barChartLegend = true;

  // events
  public chartClicked(e: any): void {
    // console.log(e);
  }


X [ERROR] TS2717: Subsequent property declarations must have the same type.  Property 'barChartPlugins' must be of type 'typeof import("D:/IT-Portal/IT-Portal/IT-Portal.UI/node_modules/chartjs-plugin-datalabels/types/index")[]', but here has type 'Plugin<keyof ChartTypeRegistry, AnyObject>[]'. [plugin angular-compiler]

    src/app/dashboard/dashboard.component.ts:221:9:
      221 │   public barChartPlugins: Plugin[] = [pluginDataLabels]
          ╵          ~~~~~~~~~~~~~~~

  'barChartPlugins' was also declared here.

    src/app/dashboard/dashboard.component.ts:176:2:
      176 │   barChartPlugins = [pluginDataLabels];
          ╵   ~~~~~~~~~~~~~~~


X [ERROR] TS2741: Property 'id' is missing in type 'typeof import("D:/IT-Portal/IT-Portal/IT-Portal.UI/node_modules/chartjs-plugin-datalabels/types/index")' but required in type 'Plugin<keyof ChartTypeRegistry, AnyObject>'. [plugin angular-compiler]

    src/app/dashboard/dashboard.component.ts:221:38:
      221 │   public barChartPlugins: Plugin[] = [pluginDataLabels]
          ╵                                       ~~~~~~~~~~~~~~~~
