NG1: Property 'categoryID' does not exist on type 'UpdateCategoryMasterComponent'. Did you mean 'categorytypId'?
        <div class="col">
          <label>Category ID</label>
         <!-- <input type="text" [(ngModel)]="updatevalue[0].categoryId" />-->
         <select [(ngModel)]="categoryID">
  <option value="" disabled selected>Select categoryName</option>
  <option *ngFor="let cldata of categoryName" [value]="cldata.itcategoryId">{{cldata.categoryName}}</option>
</select>
        </div>
