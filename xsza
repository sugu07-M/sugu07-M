 public chartClicked(event: any): void {
    if (event.active.length > 0) {
      const clickedIndex = event.active[0]._index;
      // Here you can implement logic to fetch more detailed data based on the clicked bar
      // and then update your chart data accordingly
      // For simplicity, let's just show an alert with the clicked index
      alert('Clicked index: ' + clickedIndex);
    }
  }
