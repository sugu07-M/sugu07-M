public handleDrillDownClick(event: any): void {
    if (event.active.length > 0) {
      const clickedSegment = event.active[0]._index;
      console.log("Clicked segment index:", clickedSegment);
      // Perform drill-down action here, for example:
      // this.router.navigate(['/detail', clickedSegment]);
    }
  }



  public barChartOptions: any = {
    responsive: true,
    onClick: this.handleDrillDownClick.bind(this) // Bind the click event handler
  };
