handleDrillDownClick(event: ChartEvent) {
  console.log('handleDrillDownClick method called');
  if (event.active && event.active.length > 0) {
    const clickedIndex = event.active[0]._index;
    const clickedMonth = this.month[clickedIndex];
    const completed = this.completed[clickedIndex];
    const noncompleted = this.noncompleted[clickedIndex];

    // Navigate to detail page with data
    this.router.navigate(['/detail'], { state: { month: clickedMonth, completed, noncompleted } });
  }
}
 ngOnInit() {
    this.route.paramMap.subscribe(params => {
      this.month = params.get('month');
      this.completed = +params.get('completed');
      this.noncompleted = +params.get('noncompleted');
    });
  }
  month: string;
  completed: number;
  noncompleted: number;


X [ERROR] NG5: Argument of type '{ event?: ChartEvent | undefined; active?: object[] | undefined; }' is not assignable to parameter of type 'ChartEvent'.
  Type '{ event?: ChartEvent | undefined; active?: object[] | undefined; }' is missing the following properties from type 'ChartEvent': type, native, x, y [plugin angular-compiler]

    src/app/dashboard/dashboard.component.html:245:43:
      245 │         (chartClick)="handleDrillDownClick($event)"
          ╵                                            ~~~~~~

  Error occurs in the template of component DashboardComponent.

    src/app/dashboard/dashboard.component.ts:19:15:
      19 │   templateUrl: './dashboard.component.html',
         ╵                ~~~~~~~~~~~~~~~~~~~~~~~~~~~~


X [ERROR] TS2551: Property 'active' does not exist on type 'ChartEvent'. Did you mean 'native'? [plugin angular-compiler]

    src/app/dashboard/dashboard.component.ts:234:12:
      234 │   if (event.active && event.active.length > 0) {
          ╵             ~~~~~~

  'native' is declared here.

    node_modules/chart.js/dist/types/index.d.ts:1547:2:
      1547 │   native: Event | null;
           ╵   ~~~~~~


X [ERROR] TS2551: Property 'active' does not exist on type 'ChartEvent'. Did you mean 'native'? [plugin angular-compiler]

    src/app/dashboard/dashboard.component.ts:234:28:
      234 │   if (event.active && event.active.length > 0) {
          ╵                             ~~~~~~

  'native' is declared here.

    node_modules/chart.js/dist/types/index.d.ts:1547:2:
      1547 │   native: Event | null;
           ╵   ~~~~~~


X [ERROR] TS2551: Property 'active' does not exist on type 'ChartEvent'. Did you mean 'native'? [plugin angular-compiler]

    src/app/dashboard/dashboard.component.ts:235:31:
      235 │     const clickedIndex = event.active[0]._index;
          ╵                                ~~~~~~

  'native' is declared here.

    node_modules/chart.js/dist/types/index.d.ts:1547:2:
      1547 │   native: Event | null;
           ╵   ~~~~~~
