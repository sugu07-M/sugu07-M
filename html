Argument of type '{ event?: ChartEvent | undefined; active?: object[] | undefined; }' is not assignable to parameter of type 'string'.

ngOnInit() {
    // Retrieve monthData from the router state
    this.monthData = history.state.monthData;
    console.log("Month data:", this.monthData);
  }

import { ActivatedRoute, RouterStateSnapshot } from '@angular/router';



public handleDrillDownClick(label: string): void {
    console.log("Clicked segment label:", label);
    const monthIndex = this.barChartLabels.indexOf(label);
    const monthData = {
      label: label,
      completed: this.completed[monthIndex],
      noncompleted: this.noncompleted[monthIndex]
    };
    // Navigate to a different route upon clicking a bar segment, passing the month data
    this.router.navigate(['/detail'], { state: { monthData: monthData } });
  }

core.mjs:29751 Navigation triggered outside Angular zone, did you forget to call 'ngZone.run()'?
  public barChartOptions: any = {
    responsive: true,
    onClick: this.handleDrillDownClick.bind(this) // Bind the click event handler
  };
