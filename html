ngOnInit() {
  this.route.queryParams.subscribe(params => {
    const month = params['month'];
    const status = params['status'];
    // Use month and status to filter your data
    // Display only the relevant data on this page
  });
