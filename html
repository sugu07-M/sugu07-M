ngOnInit() {
    // Retrieve monthData from the router state
    this.route.paramMap.subscribe(params => {
      this.monthData = params.get('monthData');
      console.log("Month Data in Detail Component:", this.monthData);
    });
  }
