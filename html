handleDrillDownClick(event: ChartEvent) {
  const activeElements = event?.chart?.getElementsAtEventForMode(event.native, 'point', { intersect: true });
  if (activeElements && activeElements.length > 0) {
    const clickedIndex = activeElements[0]._index;
    // Perform your logic here
  }
}
